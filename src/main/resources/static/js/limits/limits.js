 
	//判断是否有增加雇员和权限的权限（只有超级管理员有）
	/**
    if(${user.limits[1] eq 0}){
    	$("#add_user").remove();
    }
    if(${user.limits[2] eq 0}){
    	$("#add_limits").remove();
    }
    //是否显示超级管理员
    if(!${user.uid eq  1}){
    	$("#admin").text("普通管理员")
    }
    */
